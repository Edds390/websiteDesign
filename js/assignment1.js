$(document).ready(function() {
    function close_accordion() {
        // Close accordion
    }

    $('.accordion-title').click(function() {
        // get clicked accordion
        var currentAttrValue = $(this).attr('href');

        // Open and close here
    });
});
